/**
 * MSW Handlers — Combine all feature handlers here
 *
 * Import handlers from each feature file and spread them into this array.
 *
 * Example:
 * ```ts
 * import { usersHandlers } from './users'
 * import { postsHandlers } from './posts'
 *
 * export const handlers = [
 *   ...usersHandlers,
 *   ...postsHandlers,
 * ]
 * ```
 */

export const handlers = []
